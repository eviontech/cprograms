#include <stdio.h>
int main ()
{
    int number;
    scanf("%d",&number);
    for(int i=number;i>=1;i--)
    {
        for(int j=number;j>i;j--){
            printf(" ");
        }
        for(int k=1;k<=((2*i)-1);k++){
            printf("*");
        }
        printf("\n");
    }
}
